# Requirements for Streamlit Cloud deployment (demo_app.py only)
# Full project deps: see requirements-full.txt
streamlit>=1.32.0
requests>=2.31.0
urllib3>=2.0.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0
# governance policy contract loader (security/policy.py). Without it the
# guardrail degrades to allow-everything, which is silent, so keep it pinned.
PyYAML>=6.0
